// Bounce off walls
move_bounce_all(true); // Bounces off all solid objects

//direction += random_range(-20,20);
//^^ possibly gonna be replaced with code for actuall bounce control instead of it being random maybe